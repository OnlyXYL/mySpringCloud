spring:
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: config-8888
      name: service-ribbon
      profile: dev
      uri: http://localhost:8888/
      label: master